// External deps
import { FC } from 'react';

// Internal deps
import Navigator from "./src/navigation/Navigator";

const App: FC = () => {
    return (
        <Navigator />
    );
}

export default App;
